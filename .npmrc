# pnpm configuration
# Hoist packages so Vite and its plugins can resolve each other's peer deps.
shamefully-hoist=false
hoist-pattern[]=*

# Fail loudly if the wrong package manager is used.
engine-strict=true
